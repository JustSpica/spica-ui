import { Meta } from "@storybook/addon-docs";
import { space } from "@spica-ui/tokens";

import { TokensGrid } from "../components/TokensGrid";

<Meta title="Tokens/Spaces" />

# Spaces

Abaixo está a documentação detalhada de todos os espaçamentos do Design System, que deve ser usada corretamente, desde margens e preenchimentos até espaçamentos entre elementos.

<TokensGrid tokens={space} hasPixelValue hasSpaceView />
